const { Router } = require("express");
const express = require("express");
const ruta_ventas = express.Router();
const contHome = require("../controller/controller_ventas");









module.exports = ruta_ventas;